#!/bin/sh
git pull
#gut
#git submodule --quiet foreach --recursive gut
#git submodule --quiet update --init --recursive
#git submodule update
#git submodule foreach git checkout master
#git submodule foreach git pull origin master
